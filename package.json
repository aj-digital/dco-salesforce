{
  "name": "dco-salesforce-root",
  "version": "1.0.0",
  "description": "Deployment wrapper for Railway",
  "scripts": {
    "build": "cd prototype && npm install",
    "start": "node prototype/server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
